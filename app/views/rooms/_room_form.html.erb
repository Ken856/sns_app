<div class="room-form">
  <%= form_with model: Room.new, local: true do |f| %>
    <%= hidden_field_tag :user_id, @user.id %>
    <%= hidden_field_tag :room_name, @user.name %>
    <%= f.submit "TALK" %>
  <% end %>
</div>
